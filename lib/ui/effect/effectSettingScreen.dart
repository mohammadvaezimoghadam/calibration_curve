import 'package:flutter/material.dart';

class EffectSettingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xff2a6655).withOpacity(0.5),
      body: Center(
        child: Text('effect Setting Screen'),
      ),
    );
  }
}